# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode-test/

# Логи и временное
*.log
*.tmp

# Node (на случай, если появятся зависимости)
node_modules/

# Минифицированная сборка (генерируется `npm run build:min`, в репозиторий не коммитим)
custom-bg.min.js

# Playwright (интеграционный тест, улучшение 7)
test-results/
playwright-report/
.playwright/
blob-report/

# Копии, которые кладёт внутрь расширения упаковщик (scripts/package-extension.js), и сам пакет.
# Источник правды — custom-bg.js и assets/ в корне; в extension/ они появляются только на время сборки.
extension/custom-bg.js
extension/LICENSE
extension/assets/
dist/
*.vsix
